import * as React from "react";
import type { NextPage } from "next";
import { Body } from "@cebus/react-components";

const Home: NextPage = () => {
  return <Body>Hello world</Body>;
};

export default Home;
